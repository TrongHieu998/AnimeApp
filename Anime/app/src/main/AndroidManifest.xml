<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_test">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.VideoView_Activity">

        </activity>
        <activity android:name=".Activity.Like_Activity"></activity>
        <activity android:name=".Activity.Search_Activity" />
        <activity android:name=".Activity.Detail_Popular_Activity" />
        <activity android:name=".Activity.Detail" />
        <activity android:name=".Activity.HomeRecentMore" />
        <activity android:name=".BlurLayout" /> <!-- android:label="@string/title_activity_bottom_navigation" -->
        <activity android:name=".bottom_navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>